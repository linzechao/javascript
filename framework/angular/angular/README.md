Angular 学习笔记
====

```js
注意：
1、ng-bind表达式中不需要加上花括号
2、angular1.3以后，不支持控制器？
3、filter过滤对输出的所有字段有效
4、ng-click表达式里不能自增，比如“count++”
5、变量是要在controller或ng-init中声明
6、一个html中不能存在2个ng-app？
7、表单的验证前缀为form的name值
```
